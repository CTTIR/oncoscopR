% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-coerce.R
\name{.as_event01}
\alias{.as_event01}
\title{Robust event coding for Kaplan-Meier}
\usage{
.as_event01(x, mode = c("auto", "date_event", "death_event"))
}
\arguments{
\item{x}{A vector — logical, numeric, character, Date or POSIXct.}

\item{mode}{\code{"auto"} (default) treats only explicit tokens as events.
\code{"date_event"} additionally treats any non-empty, non-missing value as
an event (used for date-style columns such as recurrence date, where
presence of a date \emph{is} the event).}
}
\value{
Numeric vector of \code{0}/\code{1}/\code{NA}, the same length as \code{x}.
}
\description{
Coerces a vector to the strict \code{0}/\code{1} event coding required by
\code{\link[survival:survfit]{survival::survfit()}}. Dates (and POSIXct) map to \code{1} when present and \code{0}
when missing, useful for free-text recurrence-date columns. Strings are
parsed against an explicit, anchored token list — no prefix matching.
}
\details{
This function deliberately differs from the legacy v5 implementation in
one place: \code{"10 months"} now returns \code{NA}, not \code{1}. The original used
\code{grepl("^1", x)}, which incorrectly classified any string starting with
\code{1} as an event.
}
\seealso{
Other helpers:
\code{\link[=.add_year]{.add_year()}},
\code{\link[=.as_yesno]{.as_yesno()}},
\code{\link[=.find_col]{.find_col()}},
\code{\link[=.first_nonempty_col]{.first_nonempty_col()}},
\code{\link[=.n_distinct_nonempty]{.n_distinct_nonempty()}},
\code{\link[=.safe_date]{.safe_date()}}
}
\concept{helpers}
\keyword{internal}
