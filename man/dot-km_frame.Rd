% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{.km_frame}
\alias{.km_frame}
\title{Build the modelling frame for a Kaplan-Meier analysis}
\usage{
.km_frame(data, time_col, event_col, group_col = NULL)
}
\arguments{
\item{data}{A data frame / cohort with the time and event columns.}

\item{time_col, event_col}{Column names (character) for follow-up time and
event; the event is parsed to 0/1 via the package's hardened coercion.}

\item{group_col}{Optional grouping column (character), or \code{NULL} for one
overall curve.}
}
\value{
A list with \code{df} (columns \code{time}, \code{event}, optional \code{grp}) and
\code{n_groups}.
}
\description{
Coerces the time/event (and optional group) columns and applies the survival
guards. Raises a \code{cli} condition (never \code{shiny::validate}) on unusable input;
the server turns the condition into a \code{validate(need())} message.
}
\keyword{internal}
