% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export.R
\name{zhn_save_plot}
\alias{zhn_save_plot}
\title{Save a zhncommandR figure to PNG (600 dpi) or PDF (vector)}
\usage{
zhn_save_plot(
  plot,
  file,
  format = c("png", "pdf"),
  width = 8,
  height = 5,
  transparent = FALSE
)
}
\arguments{
\item{plot}{A \link[ggplot2:ggplot]{ggplot2::ggplot} (or other printable plot) object.}

\item{file}{Output path; written as-is.}

\item{format}{One of \code{"png"} or \code{"pdf"}.}

\item{width, height}{Dimensions in inches.}

\item{transparent}{Logical; transparent background if \code{TRUE}, else white.}
}
\value{
The \code{file} path, invisibly.
}
\description{
One save helper for every figure so resolution, device and font rules live
in one place. PNG uses \code{ragg::agg_png} (sharp text, true 600 dpi, reliable
transparency and font rendering); PDF uses \code{grDevices::cairo_pdf} (vector
output that embeds Inter — base \code{pdf()} would not). The bundled Inter font is
registered first so exported text matches the screen.
}
\examples{
p <- ggplot2::ggplot(mtcars, ggplot2::aes(factor(cyl))) +
  ggplot2::geom_bar(fill = zhn_pal$accent) + zhn_theme()
tmp <- tempfile(fileext = ".png")
zhn_save_plot(p, tmp, format = "png", width = 6, height = 4)
}
\seealso{
Other figures:
\code{\link{zhn_pal}},
\code{\link[=zhn_plot_cases_by_year]{zhn_plot_cases_by_year()}},
\code{\link[=zhn_plot_diagnoses]{zhn_plot_diagnoses()}},
\code{\link[=zhn_plot_km]{zhn_plot_km()}},
\code{\link[=zhn_register_fonts]{zhn_register_fonts()}},
\code{\link[=zhn_theme]{zhn_theme()}}
}
\concept{figures}
