% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/readers.R
\name{zhn_read_cohort}
\alias{zhn_read_cohort}
\title{Read the cohort sheet (Basisdaten / Faelle)}
\usage{
zhn_read_cohort(path, sheet = NULL, verbose = TRUE)
}
\arguments{
\item{path}{Path to the \code{.xlsx} file (typically a \code{fileInput()} temp path
or the bundled example, obtained via \code{\link[=zhn_example_path]{zhn_example_path()}}).}

\item{sheet}{Optional sheet name to override the canonical/regex resolver.}

\item{verbose}{Logical; emit a \code{cli_inform} when duplicate columns are
detected.}
}
\value{
A \link[=new_cohort_df]{cohort_df} S3 object (inheriting from
\code{data.frame}) with cleaned names, an added integer \code{behandlungsjahr}
column, and a \code{"sheet_to_use"} attribute. Empty input workbooks return
a 0-row object.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
}
\details{
Reads the primary cohort sheet from a tumour-documentation workbook,
cleans column names via \code{janitor::clean_names()}, drops trailing empty
\verb{none*} columns, and derives a \code{behandlungsjahr} (treatment year)
column from the first available date column.

Replaces the legacy \code{read_main_data()} from v5 which scanned \verb{~/Desktop}
for \code{Daten.xlsx}. The package never reads \verb{~/Desktop}.
}
\examples{
if (interactive()) {
  df <- zhn_read_cohort(zhn_example_path())
  head(df)
}
}
\seealso{
Other readers:
\code{\link[=zhn_read_diagnostics]{zhn_read_diagnostics()}},
\code{\link[=zhn_read_therapy]{zhn_read_therapy()}},
\code{\link[=zhn_read_tumorboard]{zhn_read_tumorboard()}}
}
\concept{readers}
